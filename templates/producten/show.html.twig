{% extends 'base.html.twig' %}

{% block title %}Producten{% endblock %}

{% block body %}
    <h1>Producten</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producten.id }}</td>
            </tr>
            <tr>
                <th>Productnaam</th>
                <td>{{ producten.Productnaam }}</td>
            </tr>
            <tr>
                <th>Prijs</th>
                <td>{{ producten.Prijs }}</td>
            </tr>
            <tr>
                <th>Productomschrijving</th>
                <td>{{ producten.Productomschrijving }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producten_index') }}">back to list</a>

    <a href="{{ path('producten_edit', {'id': producten.id}) }}">edit</a>

    {{ include('producten/_delete_form.html.twig') }}
{% endblock %}
